# frozen_string_literal: true

RSpec.describe Numruby do
  it 'has a version number' do
    expect(Numruby::VERSION).not_to be_nil
  end

  it 'does something useful', :skip do
    expect(false).to be(true)
  end
end
